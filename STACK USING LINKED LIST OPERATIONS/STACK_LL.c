# include <stdio.h>

struct Node{
    int data;
    struct Node * ptr;
};

int main(){
    // Now we will make a pointer which will point to the top of the element of the stack

    struct Node * Top;
    
    // Since we don't have any elements in the stack so its NULL

    Top = NULL;
    
    // WE WILL LEARN TO PUSH ELEMENT IN FURTHER UNITS OF THIS SERIES
    // THIS PROGRAM WILL NOT RUN AS THIS IS JUST CREATION OF THE STACK USING LINKED LIST
    
    
    return 0;
}